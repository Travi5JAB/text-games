// runs after /add_subcomment

// remove old comment thread
$("div#comments_thread").remove();

// render updated comment thread partial
$("<%= escape_javascript(render :partial => 'pages/part/comments') %>").appendTo("#comment_area");

// clear text field for comment area
document.getElementById('sc_ta').value='';
